# Архітектура Arc DEX

Технічна документація архітектури децентралізованої біржі на Arc Network.

## Загальний огляд системи

Arc DEX - це Automated Market Maker (AMM) DEX, що використовує модель Constant Product (x × y = k), аналогічну до Uniswap V2, але оптимізовану для специфічних особливостей Arc Network.

### Високорівнева архітектура

```
┌────────────────────────────────────────────────────────────────┐
│                       Frontend (React + ethers.js)              │
│  - Підключення гаманця (MetaMask)                              │
│  - UI для swap, додавання/видалення ліквідності                │
│  - Відображення пулів, цін, LP балансів                        │
└───────────────────────────┬────────────────────────────────────┘
                            │ JSON-RPC (HTTPS)
                            │
┌───────────────────────────▼────────────────────────────────────┐
│                    Arc Network (Layer-1 EVM)                    │
│  - Під-секундна фінальність                                    │
│  - Стабількоін-газ (USDC)                                      │
│  - EVM-сумісність                                              │
└───────────────────────────┬────────────────────────────────────┘
                            │
         ┌──────────────────┼──────────────────┐
         │                  │                  │
         ▼                  ▼                  ▼
  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐
  │  DEXCore    │───▶│  LPToken    │    │  Whitelist  │
  │  Contract   │    │  Contract   │    │  Contract   │
  └─────────────┘    └─────────────┘    └─────────────┘
         │
         │ Зберігає резерви, управляє пулами
         ▼
  ┌─────────────────────────────────────┐
  │         Liquidity Pools             │
  │  Pool 1: USDC/WETH                  │
  │  Pool 2: USDC/DAI                   │
  │  Pool 3: WETH/DAI                   │
  │  ...                                │
  └─────────────────────────────────────┘
```

## Компоненти смарт-контрактів

### 1. DEXCore.sol

Головний контракт, що управляє всією логікою DEX.

#### Структури даних

```solidity
struct Pool {
    address tokenA;           // Перший токен у парі (sorted address)
    address tokenB;           // Другий токен у парі (sorted address)
    uint256 reserveA;         // Резерв токена A
    uint256 reserveB;         // Резерв токена B
    address lpToken;          // Адреса LP токена для цього пулу
    uint256 totalLiquidity;   // Загальна кількість LP токенів
    bool exists;              // Чи існує пул
}

// Маппінг poolId → Pool
mapping(bytes32 => Pool) public pools;

// Список всіх poolId для ітерації
bytes32[] public poolIds;
```

#### Ключові функції

**createPool(tokenA, tokenB) → poolId**
- Створює новий пул ліквідності для торгової пари
- Перевіряє whitelist обох токенів
- Сортує адреси (token0 < token1)
- Генерує унікальний poolId: `keccak256(token0, token1)`
- Деплоїть новий LP токен контракт
- Зберігає Pool структуру

**addLiquidity(tokenA, tokenB, amountA, amountB, minLiquidity) → liquidity**
- Додає ліквідність до існуючого пулу
- Трансферить токени від користувача до контракту
- Розраховує кількість LP токенів:
  - Перший депозит: `liquidity = √(amountA × amountB) - MINIMUM_LIQUIDITY`
  - Наступні: `liquidity = min(amountA × totalLP / reserveA, amountB × totalLP / reserveB)`
- Mint LP токени користувачу
- Оновлює резерви пулу

**removeLiquidity(tokenA, tokenB, liquidity, minAmountA, minAmountB) → (amountA, amountB)**
- Видаляє ліквідність з пулу
- Розраховує суми токенів пропорційно до частки LP токенів
- Burn LP токени користувача
- Трансферить токени назад користувачу
- Оновлює резерви пулу

**swap(tokenIn, tokenOut, amountIn, minAmountOut) → amountOut**
- Обмінює один токен на інший
- Розраховує вихідну суму за формулою AMM
- Враховує swap комісію (0.3%)
- Трансферить токени
- Оновлює резерви
- Емітить Swap event

**getAmountOut(tokenIn, tokenOut, amountIn) → amountOut**
- View функція для розрахунку очікуваної вихідної суми
- Не змінює стан контракту
- Використовується frontend для відображення цін

#### Адміністративні функції

**setSwapFee(newFee)**
- Оновлює комісію за swap
- Обмеження: `newFee <= MAX_SWAP_FEE` (100 = 1%)

**pause() / unpause()**
- Призупиняє/відновлює всі операції DEX
- Emergency функція

#### Константи та параметри

```solidity
uint256 public swapFee = 30;              // 0.3% (30 / 10000)
uint256 public constant MAX_SWAP_FEE = 100;     // 1% максимум
uint256 public constant FEE_DENOMINATOR = 10000;
uint256 public constant MINIMUM_LIQUIDITY = 1000; // 1000 wei lock
```

### 2. LPToken.sol

ERC-20 токен, що представляє частку провайдера ліквідності у пулі.

#### Особливості

```solidity
contract LPToken is ERC20, Ownable {
    address public tokenA;  // Токен A у парі
    address public tokenB;  // Токен B у парі

    function mint(address to, uint256 amount) external onlyOwner;
    function burn(address from, uint256 amount) external onlyOwner;
}
```

- **Owner:** DEXCore контракт (має exclusive mint/burn права)
- **Трансферабельний:** Користувачі можуть передавати LP токени
- **Назва:** Автогенерована, наприклад "USDC-WETH LP"
- **Символ:** Наприклад "LP-USDC-WETH"

#### Життєвий цикл LP токена

```
1. DEXCore.createPool()
   └─> Деплой нового LPToken контракту

2. DEXCore.addLiquidity()
   └─> LPToken.mint(user, liquidity)

3. Користувач тримає LP токени
   └─> Може transfer до іншої адреси
   └─> Може додати як collateral (у майбутніх протоколах)

4. DEXCore.removeLiquidity()
   └─> LPToken.burn(user, liquidity)
```

### 3. TokenWhitelist.sol

Контроль списку дозволених токенів для торгівлі.

#### Структура даних

```solidity
struct TokenInfo {
    bool isWhitelisted;
    string name;
    string symbol;
    uint8 decimals;
    uint256 addedAt;  // Timestamp додавання
}

mapping(address => TokenInfo) public tokens;
address[] public tokenList;  // Для ітерації
```

#### Функції

**addToken(token, name, symbol, decimals)**
- Додає токен до whitelist
- Зберігає метадані для UI
- Емітить TokenWhitelisted event

**addTokensBatch(tokens[], names[], symbols[], decimals[])**
- Додає декілька токенів за один виклик
- Оптимізація газу для початкового налаштування
- Пропускає дублікати

**removeToken(token)**
- Видаляє токен з whitelist
- Не впливає на існуючі пули (immutable)
- Запобігає створенню нових пулів з цим токеном

**isWhitelisted(token) → bool**
- Перевірка статусу токена
- Використовується DEXCore при створенні пулів

## AMM математика

### Модель Constant Product

Arc DEX використовує формулу **x × y = k**, де:
- `x` = reserveA (резерв токена A)
- `y` = reserveB (резерв токена B)
- `k` = константа добутку (product constant)

#### Інваріант

Після кожної операції має виконуватися:
```
reserveA_after × reserveB_after >= reserveA_before × reserveB_before
```

Рівність порушується тільки через додавання комісій, які збільшують `k`.

### Ціноутворення

Ціна визначається відношенням резервів:

```
priceA_in_B = reserveB / reserveA
priceB_in_A = reserveA / reserveB
```

**Приклад:**
```
Pool: 10,000 USDC / 10 WETH
Price WETH in USDC = 10,000 / 10 = 1,000 USDC per WETH
Price USDC in WETH = 10 / 10,000 = 0.001 WETH per USDC
```

### Розрахунок Swap

#### Без комісії (теоретично):
```
(reserveIn + amountIn) × (reserveOut - amountOut) = k
amountOut = (reserveOut × amountIn) / (reserveIn + amountIn)
```

#### З комісією 0.3%:
```solidity
uint256 amountInWithFee = amountIn × (FEE_DENOMINATOR - swapFee);
// = amountIn × 9970 / 10000

uint256 numerator = reserveOut × amountInWithFee;
uint256 denominator = reserveIn × FEE_DENOMINATOR + amountInWithFee;

amountOut = numerator / denominator;
```

**Математично:**
```
amountInAfterFee = amountIn × 0.997
amountOut = (reserveOut × amountInAfterFee) / (reserveIn + amountInAfterFee)
```

#### Приклад розрахунку

```
Початковий стан:
- Pool: 10,000 USDC / 10 WETH
- k = 100,000
- Price: 1 WETH = 1,000 USDC

Swap: 100 USDC → ? WETH
1. З комісією: 100 × 0.997 = 99.7 USDC ефективно
2. amountOut = (10 × 99.7) / (10,000 + 99.7)
             = 997 / 10,099.7
             = 0.09871 WETH

3. Новий стан:
   - reserveA = 10,100 USDC
   - reserveB = 9.90129 WETH
   - k' = 10,100 × 9.90129 ≈ 100,003 (збільшилося через комісію!)

4. Нова ціна: 10,100 / 9.90129 = 1,020 USDC per WETH
   (ціна WETH зросла через купівлю)
```

### Slippage (ковзання ціни)

Slippage - це різниця між очікуваною та реальною ціною.

**Формула:**
```
slippage = (expectedPrice - executedPrice) / expectedPrice × 100%
```

**Фактори slippage:**
1. **Розмір trade відносно пулу:**
   - Великі trade → великий slippage
   - Малі trade → малий slippage

2. **Розмір пулу:**
   - Великі пули → менший slippage
   - Малі пули → більший slippage

**Приклад:**
```
Pool: 10,000 USDC / 10 WETH

Swap 100 USDC (1% від пулу):
- Slippage: ~1%

Swap 1,000 USDC (10% від пулу):
- Slippage: ~10%

Swap 5,000 USDC (50% від пулу):
- Slippage: ~50% (дуже дорого!)
```

**Захист:** Параметр `minAmountOut` у функції `swap()`.

### Розрахунок ліквідності

#### Перший депозит (пул порожній):

```solidity
liquidity = sqrt(amountA × amountB) - MINIMUM_LIQUIDITY;

// Перші 1000 units спалюються назавжди на address(1)
lpToken.mint(address(1), MINIMUM_LIQUIDITY);
lpToken.mint(user, liquidity);
```

**Чому `sqrt()`?**
- Забезпечує пропорційність для обох токенів
- Геометричне середнє (справедливе для обох сторін)
- Уникає маніпуляції ціною

**Чому MINIMUM_LIQUIDITY?**
- Запобігає атакам при малій ліквідності
- Робить пул економічно стабільнішим
- Industry standard (Uniswap V2)

#### Наступні депозити:

```solidity
uint256 liquidityA = (amountA × totalLiquidity) / reserveA;
uint256 liquidityB = (amountB × totalLiquidity) / reserveB;
liquidity = min(liquidityA, liquidityB);
```

**Чому min()?**
- Захист провайдера від imbalanced додавання
- Гарантує пропорційність
- Запобігає донації резервів іншим LP

#### Приклад:

```
Pool: 10,000 USDC / 10 WETH, totalLP = 1,000

Додати: 1,000 USDC + 1 WETH
liquidityA = (1,000 × 1,000) / 10,000 = 100 LP
liquidityB = (1 × 1,000) / 10 = 100 LP
Отримано: 100 LP (пропорційно)

Додати: 1,000 USDC + 0.5 WETH (imbalanced!)
liquidityA = (1,000 × 1,000) / 10,000 = 100 LP
liquidityB = (0.5 × 1,000) / 10 = 50 LP
Отримано: 50 LP (мінімум, захист користувача)
```

### Видалення ліквідності

```solidity
amountA = (liquidity × reserveA) / totalLiquidity;
amountB = (liquidity × reserveB) / totalLiquidity;
```

Пропорційне виведення відносно частки у пулі.

**Приклад:**
```
Pool: 10,000 USDC / 10 WETH, totalLP = 1,000
Користувач має: 100 LP (10% від пулу)

Видалити 100 LP:
amountA = (100 × 10,000) / 1,000 = 1,000 USDC
amountB = (100 × 10) / 1,000 = 1 WETH

Отримано: 1,000 USDC + 1 WETH (10% резервів)
```

## Потоки виконання

### Повний життєвий цикл пулу

```
┌─────────────────────────────────────────────────────────────┐
│ 1. СТВОРЕННЯ ПУЛУ (createPool)                               │
│                                                              │
│ Admin →  DEXCore.createPool(USDC, WETH)                     │
│          ├─> Перевірка whitelist                            │
│          ├─> Сортування адрес (token0, token1)              │
│          ├─> Генерація poolId = keccak256(token0, token1)   │
│          ├─> Деплой LPToken("USDC-WETH LP", "LP-USDC-WETH") │
│          └─> Збереження Pool структури                      │
│                                                              │
│ Результат: Порожній пул готовий до ліквідності              │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│ 2. ДОДАВАННЯ ПОЧАТКОВОЇ ЛІКВІДНОСТІ (addLiquidity)          │
│                                                              │
│ User1 → Approve USDC & WETH                                  │
│      → DEXCore.addLiquidity(                                │
│           USDC, WETH,                                        │
│           10000 USDC, 10 WETH,                              │
│           minLiquidity = 0                                   │
│        )                                                     │
│        ├─> Transfer 10000 USDC від User1 до DEXCore        │
│        ├─> Transfer 10 WETH від User1 до DEXCore           │
│        ├─> liquidity = sqrt(10000 × 10) - 1000             │
│        │              = sqrt(100,000) - 1000                │
│        │              = 316.23 - 1000 = нема... помилка?   │
│        │   (Потрібно більше токенів для початку!)          │
│        │                                                     │
│        │   Правильний приклад:                              │
│        ├─> liquidity = sqrt(10,000,000 × 10,000) - 1000    │
│        │              = sqrt(100,000,000,000) - 1000        │
│        │              = 316,227 - 1000 = 315,227 LP         │
│        ├─> Mint 1000 LP → address(1) (burn forever)        │
│        ├─> Mint 315,227 LP → User1                         │
│        ├─> reserveA = 10,000,000                           │
│        └─> reserveB = 10,000                                │
│                                                              │
│ Результат: Пул ініціалізовано, User1 має 315,227 LP        │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│ 3. ДОДАВАННЯ ЛІКВІДНОСТІ ІНШИМ КОРИСТУВАЧЕМ                 │
│                                                              │
│ User2 → DEXCore.addLiquidity(                               │
│           USDC, WETH,                                        │
│           1,000,000 USDC, 1000 WETH,                        │
│           minLiquidity = 31000                               │
│        )                                                     │
│        ├─> liquidityA = (1,000,000 × 316,227) / 10,000,000 │
│        │              = 31,622.7 LP                         │
│        ├─> liquidityB = (1000 × 316,227) / 10,000          │
│        │              = 31,622.7 LP                         │
│        ├─> liquidity = min(31,622.7, 31,622.7) = 31,622 LP │
│        ├─> Mint 31,622 LP → User2                          │
│        ├─> reserveA = 11,000,000 USDC                      │
│        └─> reserveB = 11,000 WETH                          │
│                                                              │
│ Результат: Пул виріс, User2 має ~10% частку                │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│ 4. SWAP (ОБМІН)                                             │
│                                                              │
│ User3 → Approve 100,000 USDC                                │
│      → DEXCore.swap(                                        │
│           tokenIn = USDC,                                    │
│           tokenOut = WETH,                                   │
│           amountIn = 100,000 USDC,                          │
│           minAmountOut = 990 WETH (1% slippage tolerance)   │
│        )                                                     │
│        ├─> Transfer 100,000 USDC від User3 до DEXCore      │
│        ├─> Розрахунок:                                      │
│        │   amountInWithFee = 100,000 × 9970 / 10000        │
│        │                    = 99,700 USDC                   │
│        │   amountOut = (11,000 × 99,700) /                 │
│        │               (11,000,000 + 99,700)                │
│        │             = 1,096,700,000 / 11,099,700           │
│        │             = 98.79 WETH                            │
│        ├─> Перевірка: 98.79 >= 990? НІ! Slippage exceeded  │
│        │                                                     │
│        │   (User3 повинен збільшити slippage або           │
│        │    зменшити розмір trade)                         │
│        │                                                     │
│        │   Виправлений swap з 10,000 USDC:                 │
│        │   amountOut = (11,000 × 9,970) /                  │
│        │               (11,000,000 + 9,970)                 │
│        │             = 109,670,000 / 11,009,970             │
│        │             = 9.96 WETH                             │
│        ├─> Transfer 9.96 WETH від DEXCore до User3         │
│        ├─> reserveA = 11,010,000 USDC (↑)                  │
│        ├─> reserveB = 11,000 - 9.96 = 10,990.04 WETH (↓)  │
│        └─> Нова ціна: 11,010,000 / 10,990.04 ≈ 1,001.82   │
│            (WETH подорожчав через купівлю)                  │
│                                                              │
│ Результат: User3 обміняв USDC на WETH, ціна змінилась      │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│ 5. ВИДАЛЕННЯ ЛІКВІДНОСТІ (removeLiquidity)                  │
│                                                              │
│ User1 → Approve 315,227 LP                                   │
│      → DEXCore.removeLiquidity(                             │
│           USDC, WETH,                                        │
│           liquidity = 315,227 LP (вся частка),              │
│           minAmountA = 0,                                    │
│           minAmountB = 0                                     │
│        )                                                     │
│        ├─> totalLiquidity = 316,227 + 31,622 = 347,849     │
│        ├─> amountA = (315,227 × 11,010,000) / 347,849      │
│        │           = 9,972,000 USDC                         │
│        ├─> amountB = (315,227 × 10,990.04) / 347,849       │
│        │           = 9,965 WETH                              │
│        ├─> Burn 315,227 LP від User1                       │
│        ├─> Transfer 9,972,000 USDC до User1                │
│        ├─> Transfer 9,965 WETH до User1                    │
│        ├─> reserveA = 1,038,000 USDC                       │
│        └─> reserveB = 1,025.04 WETH                        │
│                                                              │
│ Результат: User1 вийшов з пулу, отримав назад токени +     │
│            частку від комісій (заробив ~0.3% від swap)     │
└─────────────────────────────────────────────────────────────┘
```

## Інтеграція з Arc Network

### 1. Стабількоін-газ (USDC)

**Як працює:**
```javascript
// Стандартна Ethereum транзакція:
{
  from: userAddress,
  to: dexAddress,
  data: encodedFunctionCall,
  gasPrice: "20 gwei",  // У wei (ETH)
  gasLimit: 200000
}
// Вартість: gasPrice × gasUsed (в ETH)

// Arc Network транзакція:
{
  from: userAddress,
  to: dexAddress,
  data: encodedFunctionCall,
  gasPrice: "0.001 USDC",  // У USDC units
  gasLimit: 200000
}
// Вартість: gasPrice × gasUsed (в USDC!)
```

**Імплікації для DEX:**
- Користувачі мають мати USDC для транзакцій
- Передбачувана вартість у доларах
- Можливість використання DEX для топ-апу USDC балансу

### 2. Під-секундна фінальність

**Порівняння:**
```
Ethereum Mainnet:
User → Submit tx
     → Pending (0-15 min)
     → Confirmed in block
     → Wait ~12-15 min (finality)
     → Transaction final

Arc Network:
User → Submit tx
     → Confirmed in block (~0.5s)
     → Transaction final
```

**Переваги для DEX:**
- Швидкі swap без очікування
- Кращий UX (no pending state)
- Менше часу для front-running
- Можливість high-frequency trading

### 3. EVM-сумісність

**Що працює:**
- ✅ Solidity контракти
- ✅ Hardhat, Truffle, Foundry
- ✅ Ethers.js, Web3.js
- ✅ MetaMask, WalletConnect
- ✅ The Graph (indexing)
- ✅ OpenZeppelin libraries

**Налаштування MetaMask для Arc:**
```javascript
// Add Arc Testnet
await window.ethereum.request({
  method: 'wallet_addEthereumChain',
  params: [{
    chainId: '0x122F',  // 4655 в hex
    chainName: 'Arc Testnet',
    nativeCurrency: {
      name: 'USDC',
      symbol: 'USDC',
      decimals: 6  // USDC має 6 decimals
    },
    rpcUrls: ['https://rpc-testnet.arc.network'],
    blockExplorerUrls: ['https://explorer-testnet.arc.network']
  }]
});
```

## Event-driven архітектура

### Events для моніторингу та індексації

```solidity
// DEXCore.sol Events
event PoolCreated(
    bytes32 indexed poolId,
    address indexed tokenA,
    address indexed tokenB,
    address lpToken
);

event LiquidityAdded(
    bytes32 indexed poolId,
    address indexed provider,
    uint256 amountA,
    uint256 amountB,
    uint256 liquidity
);

event LiquidityRemoved(
    bytes32 indexed poolId,
    address indexed provider,
    uint256 amountA,
    uint256 amountB,
    uint256 liquidity
);

event Swap(
    bytes32 indexed poolId,
    address indexed trader,
    address tokenIn,
    address tokenOut,
    uint256 amountIn,
    uint256 amountOut
);

event SwapFeeUpdated(uint256 oldFee, uint256 newFee);
```

### Використання для аналітики

**The Graph субграф (приклад):**
```graphql
type Pool @entity {
  id: ID!
  tokenA: Token!
  tokenB: Token!
  reserveA: BigInt!
  reserveB: BigInt!
  totalLiquidity: BigInt!
  volumeUSD: BigDecimal!
  txCount: BigInt!
}

type Swap @entity {
  id: ID!
  pool: Pool!
  trader: Bytes!
  tokenIn: Token!
  tokenOut: Token!
  amountIn: BigInt!
  amountOut: BigInt!
  timestamp: BigInt!
}
```

## Оптимізації газу

### Storage patterns

```solidity
// ❌ Неоптимізовано:
struct Pool {
    address tokenA;      // 20 bytes
    bool exists;         // 1 byte (але займає 32 bytes slot!)
    address tokenB;      // 20 bytes
    uint256 reserveA;    // 32 bytes
}
// Загалом: 4 slots × 20000 gas = 80000 gas

// ✅ Оптимізовано (packing):
struct Pool {
    address tokenA;      // 20 bytes ┐
    address tokenB;      // 20 bytes │ Slot 1 (не поміститься)
    uint256 reserveA;    // 32 bytes   Slot 2
    uint256 reserveB;    // 32 bytes   Slot 3
    address lpToken;     // 20 bytes ┐
    bool exists;         // 1 byte   │ Slot 4
    uint256 totalLiquidity; // 32 bytes Slot 5
}
```

### Batch operations

```solidity
// ❌ Додавання токенів по одному:
for (uint i = 0; i < 10; i++) {
    whitelist.addToken(tokens[i], ...);  // 10 транзакцій
}

// ✅ Batch додавання:
whitelist.addTokensBatch(tokens, names, symbols, decimals); // 1 транзакція
```

### View функції (off-chain)

```solidity
// Розрахунок amountOut не змінює стан
function getAmountOut(...) external view returns (uint256) {
    // No gas cost when called off-chain!
}
```

## Масштабування

### Horizontal scaling (більше пулів)

```
DEXCore підтримує необмежену кількість пулів:
- USDC/WETH
- USDC/DAI
- USDC/USDT
- WETH/DAI
- ... (тисячі пулів)

Кожен пул незалежний:
- Окремі резерви
- Окремий LP токен
- Окремі комісії (у майбутньому)
```

### Layer-2 та cross-chain (майбутнє)

```
Arc DEX (Layer-1)
      ↕
Arc Bridge Protocol
      ↕
Ethereum Mainnet DEX

Можливості:
- Cross-chain liquidity
- Unified LP позиції
- Арбітраж між ланцюгами
```

## Висновок

Arc DEX архітектура поєднує перевірені AMM patterns (Uniswap V2) з унікальними перевагами Arc Network:
- ⚡ Під-секундна фінальність
- 💵 Стабількоін-газ
- 🏦 Інституційна готовність
- 🔒 Multiple security layers

Модульна архітектура дозволяє легко розширювати функціональність та інтегруватися з іншими протоколами.

---

**Наступні кроки:**
1. Вивчити [README.md](./README.md) для інструкцій з розгортання
2. Переглянути [SECURITY.md](./SECURITY.md) для аналізу ризиків
3. Розгорнути на Arc Testnet та протестувати
